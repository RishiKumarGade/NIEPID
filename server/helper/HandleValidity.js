module.exports.HandleValidityofTeacher = (key, value)=>{
    // TODO check validity of info
    return true
}

module.exports.HandleValidityofStudent = (key , value)=>{

    // TODO check validity of info just check if the value and key are correct or not , say if the key is aadhar check if value is number and contains 12 digits,if no return false , check others like wise
    return true
}